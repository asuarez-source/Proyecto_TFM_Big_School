import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.estudiotecnico.app',
  appName: 'Estudios técnicos',
  webDir: 'dist/client/browser',
};

export default config;
