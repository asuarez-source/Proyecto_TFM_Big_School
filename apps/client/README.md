# Cliente

Aplicación Ionic/Angular preparada para Capacitor. La primera pantalla consulta candidatos del catálogo CANBus mediante la API y no ejecuta reglas autoritativas en el frontend.

Durante el desarrollo, `/api` se reenvía a `http://localhost:3000`. La configuración y los comandos reproducibles se describen en `docs/procedures/development.md`.
